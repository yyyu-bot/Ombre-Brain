# ============================================================
# Ombre Brain Python 依赖
# 安装: pip install -r requirements.txt
# ============================================================

# MCP 协议 SDK（Claude 通信核心）
mcp>=1.0.0

# 模糊匹配（记忆桶搜索）
rapidfuzz>=3.0.0

# OpenAI 兼容客户端（支持 DeepSeek/Ollama/LM Studio/vLLM/Gemini 等任意兼容 API）
openai>=1.0.0

# YAML 配置解析
pyyaml>=6.0
types-PyYAML>=6.0

# Markdown frontmatter 解析（桶文件读写）
python-frontmatter>=1.1.0

# 异步 HTTP 客户端（应用层保活 ping）
httpx>=0.27.0

# 向量相似度计算 (导入模式/聚类)
numpy>=1.24.0
scikit-learn>=1.2.0

# === 向量化后端 ===
# 2.0.3 起：默认 backend=local（fastembed + bge-m3）。fastembed 是 ONNX 推理，
# 不依赖 torch，安装只有 ~50MB；bge-m3 模型权重首次启动自动下载到 models/bge-m3/。
# 若只走远程 API（embedding.backend=api），仍需要装 fastembed（包体积小，不占运行内存如果不调用）。
fastembed>=0.4.0

# === 已废弃：旧的 sentence-transformers 本地后端 ===
# 2.0.3 不再推荐。requirements-local.txt 已棄用，仅保留作历史参考。
